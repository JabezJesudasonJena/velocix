import React from 'react';
import CreateStoreForm from '@/components/Pages/store/CreateStoreForm'; // Adjust path if necessary

export default function CreateStorePage() {
  return (
    <main>
      <CreateStoreForm />
    </main>
  );
}